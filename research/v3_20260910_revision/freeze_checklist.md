# 修订后的重新冻结步骤

状态：prepared_not_refrozen。这里只把实施与验收顺序写清，尚未修改引擎、冻结新版本或运行新诊断。

1. **审阅候选机制。** 确认建成、投运、物理生效、行政验收分别记录，审阅角色2投运、0.5技术成本转移、2轮前置时滞和存量问题处理等合成假设。来源不能支持的值保留探索性标记。不得因为旧001结果没有验收，就给其历史数据补记新规物理生效。
2. **完成独立接口检验。** 使用本包冻结的6种旧V3观察、两版说明和48个独立请求，按判读规程分别报告理解与选择。收费授权、执行器接入、账本和云端检查须先到位；本包没有完成此步骤。即使模型仍不采用B/C，也不自动修改成功条件或奖励。
3. **按明确决定实施新版本。** 在新候选版本中实现投运动作和物理/知识/行政字段；说明文本只按已有操作语义澄清。保存旧8530b527…代码快照和原结果，不把新规混入旧运行；变化清单明确记录动作机会、决策劳动、技术劳动承担者及观察字段变更。
4. **运行针对性离线验收。** 将状态JSON中11例实现为真实可执行测试，并覆盖成本守恒、未授权角色不可见/不可执行、普通与强化相同技术规则、缺回执不改写实际完成、技术失败留成本、生效后行政撤项不回滚、轮界时点及旧问题不自动清零。规格中的expected不是已验证结果；测试运行通过后才另记actual和证据。
5. **进行框架集成检验。** 明确分开本地纯逻辑通过、云端官方框架整步通过、checkpoint恢复通过及真实模型通过。先做无需付费模型的脚本化整步与恢复验证。若改动预算或证据校验代码，运行相应单元测试；不把单主体通过当作整套通过。
6. **冻结新设计、再报价诊断。** 生成新代码、提示、输入、情景、模型设置和分析规程的哈希清单。根据届时价格、期限及可用资源，单独报价6个自主诊断及允许尝试数。只有明确授权后执行；技术失败保留部分证据并停止，不自行增额或反复重试。
7. **诊断完成后再决定32组预实验。** 六个诊断须有完整证据且技术有效；采用为零是业务结果，不能自动剔除。新旧机制的轨迹不混为同一处理版本。32组仍属后续独立阶段，当前没有授权和运行，也不能提前冻结正式重复次数。

每一步产出短表：完成项、证据路径与哈希、未验证项、失败/偏离及处理理由。读者由此能区分“规程写好了”“程序实现了”“离线通过了”和“真实模型运行了”。
