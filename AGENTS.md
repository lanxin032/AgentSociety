# AgentSociety：政策组合研究与云端执行

## 项目定位与入口
- 本地目录为 `D:/AgentSociety`，云端开发目录为 `/home/coder/policy-mix`；连接信息和现有命令见根目录 `README.md`。
- 当前工作包含政策条款到模拟操作的研究映射，以及官方 AgentSociety 框架的云端执行验证。不要将准备阶段或 smoke test 表述为正式研究完成。
- 优先阅读与任务相关的 `research/` 材料、`tools/` 入口、`tests/` 和 `runs/` 证据；报告中的日期和版本只代表该次运行，使用时核验当前状态。
- 源代码只维护项目根目录的一份，在原文件上修改；不为每轮修订复制版本目录。清理重复实现前检查引用，保留仍被使用的共用模块和历史实验原始证据。

## 研究设计与证据
- 政策映射入口为 `research/policy_mapping_20260909/mapping.json` 与 `sources.json`。先核对是否存在用户指定的新版本。
- 区分政策原文事实、工具分类、机制推断、参数设计和模拟结果。代表性政策样本不能表述为完整政策总体或数量分布。
- 为措施保留来源、条款定位、操作主体、基准情景、强化操作、强度定义、前提、结果指标及校准依据。
- 工具类型不等于实施强度；政务申请、热线公共问题与内部报表场景不得未经论证合并。
- 先确定研究问题、分析单位、机制及可观察指标，再扩充智能体数量或情景。对照组保持合法且现实的基准条件。
- 政策处理改变信息、规则、资源或流程，不直接把满意度、效能等结果设为更优。主体只能使用其合法可见信息，不能读取隐藏真实资格等全知状态。
- 组合实验先审查替代关系、共同基础条件、资源约束及作用重复；强度与顺序分别操作化。未经校准的值标为探索性假设。
- 正式实验记录模型、提示、参数、情景、随机种子或随机性控制、重复次数、指标和输入版本；模拟差异不能直接声称为现实因果效应。

## 云端环境与同步
- 连接复用 `tools/coder.ps1` 和现有 Coder 登录，不复制或打印令牌。AgentSociety 执行使用云端 `/usr/local/bin/python`，离线证据检查可用现有本地 Python。
- 框架位于 `/app/packages/agentsociety2`；优先使用官方类和接口。除非任务明确需要，不改预装框架、不重建仿真引擎、不重装本地 WSL 或 Conda。
- 本地和云端没有自动双向同步；覆盖前比较文件和哈希，明确修改来源，避免覆盖远端新改动。
- 长任务前查询当前停止期限和可用资源。后台运行不保证工作区持续在线；不要为验证存储而擅自重启。云端独有结果回收并核对哈希后再作为持久交付。
- 区分包元数据版本、模块版本、离线导入成功、API 可用、单主体成功、整步成功和查询成功，不把局部通过报告为完整通过。

## API 预算与运行安全
- 真实 API 与仿真使用当前任务明确授权的额度及现有预算控制。README 中旧测试的额度不能作为新实验授权；有有效额度时不重复询问。
- 运行前检查对应预算账本及剩余额度；不得删除、重置账本或新建控制器绕过限制。预算耗尽后继续离线分析，收费执行等待新额度。
- 密钥保留在既有云端秘密配置中，排除在代码、日志、结果下载和提示之外。保留 loopback 代理及框架安全检查。
- 不并发启动多个 smoke/API 控制器；统一记账和服务归属。请求数、已报告 token、估算费用与实际账单分别说明。

## 验证与子智能体分工
- 脚本：哈希、预算运算、证据结构校验；Luna max：按已定模板抽取政策和整理日志；Astra low：接口对照和局部诊断；Astra medium 及以上：机制、实验设计、冲突与关键审查。沿用全局升级规则。
- 文献或日志读取可并行；云端上传、Ray 生命周期、正式运行和预算账本由单一负责人管理。子智能体分别写候选文件，主智能体统一合并。
- 改预算或证据验证代码时运行相关 `tests/` 单元测试。完整离线测试入口为 `python -m unittest discover -s tests -v`，在已确认的环境及项目目录执行。
- `tools/check_remote.py` 只验证有限的离线环境条件，不能证明真实模型访问；`tools/verify_smoke.py` 用于已保存证据的离线核验。
- `tools/run_smoke.py` 属于真实收费执行，按预算约定运行；修改查询代码后，未重新执行就明确写“尚未实测”。仅修改说明文件不启动真实 smoke test。
